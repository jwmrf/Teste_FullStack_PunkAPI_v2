{
  "name": "fullstack_punkapi",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "back": "npm run start --prefix ./back",
    "front": "npm run dev --prefix ./front",
    "install_back": "npm run start --prefix ./back",
    "install_front": "npm run dev --prefix ./front",
    "app": "concurrently \"npm run back\" \"npm run front\"",
    "fullinstall": "concurrently \"npm run install_back\" \"npm run install_front\""
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  },
  "devDependencies": {
  }
}
